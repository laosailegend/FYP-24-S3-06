import fs from 'fs/promises';
import { LambdaBuildArgs } from './args';
import { build } from 'esbuild';
declare type FileWriter = typeof fs.writeFile;
declare type LogWriter = typeof console.log;
declare type Build = typeof build;
export declare class CliHandler {
    private writeFile;
    private writeLog;
    private build;
    constructor(writeFile: FileWriter, writeLog: LogWriter, build: Build);
    handle: (options: LambdaBuildArgs) => Promise<void>;
}
export {};
