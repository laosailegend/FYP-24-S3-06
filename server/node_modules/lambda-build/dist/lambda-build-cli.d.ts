/// <reference types="node" />
import { promises as fsPromises } from 'fs';
import { LambdaBuildCore } from './lambda-build-core';
export declare const ZIP_FILE_NAME = "archive.zip";
export declare const META_FILE_NAME = "meta.json";
declare type LogWriter = {
    log: typeof console.log;
};
interface LambdaBuildArgs {
    entry: string;
    external: string[];
    metafile: boolean;
    region?: string;
    lambdas?: string[];
}
export declare class LambdaBuildCli {
    private fsys;
    private output;
    private lambdaBuildCore;
    constructor(fsys: typeof fsPromises, output: LogWriter, lambdaBuildCore: LambdaBuildCore);
    commandArchive: (options: LambdaBuildArgs) => Promise<void>;
    private publishAndLog;
    commandUpload: (options: LambdaBuildArgs) => Promise<void>;
    outputErrors: (err: unknown) => void;
}
export {};
